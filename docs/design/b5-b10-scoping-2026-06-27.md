# B5–B10 scoping — 2026-06-27 (HEAD bb582aa97, branch `next`)

Scope-and-verify pass over the six open v1 hard blockers in
[`v1-release-exemplary-gate.md`](v1-release-exemplary-gate.md). The goal is
not to close them; it is to establish, against the filesystem and by running
each gate's own recorded command, **exactly what work remains** for each.

The gate's binding rule is: *a gate is closed only with in-tree evidence that
passes, and B7/B8/B9 results must be part of the release path, not a manual
one-off.* So "the script exists and passes locally" is necessary but not
always sufficient — for the gates that demand a CI-enforced result, the script
existing is only half the bar.

## Method

Every claim below was verified at HEAD `bb582aa97`:

- Referenced scripts/dirs checked for existence (all present).
- Each gate's recorded command was run locally and its exit captured.
- CI wiring checked by grepping `.github/workflows/{ci,release,dependencies}.yml`.

Tooling present on this machine: Go 1.25/1.26 toolchain, `node` v20 (PATH) and
v24.14.1 (nvm, used by the conformance/interop scripts), `npx`/`npm` 10.8.2.

## Bottom line

| Blocker | Artifacts exist | Local command passes | CI/release-wired | Remaining work |
|---|---|---|---|---|
| B5 security evidence | ✅ | ✅ | ✅ (`-race` in ci.yml) | **None substantive** — confirm SECURITY.md claim-map is current; flip gate to closed |
| B6 dep contract | ✅ | ✅ | ✅ (ci.yml:128) | **None** — closeable now |
| B7 conformance | ✅ | ✅ (dry-run; self-fixture) | ❌ **not in any workflow** | Wire `mcp-conformance.sh` into CI or release; record a passing run artifact |
| B8 non-Go interop | ✅ | ✅ (TS stdio smoke green) | ❌ **not in any workflow** | Decide transport scope; cover in-scope transports; wire into CI/release |
| B9 perf baseline | ✅ | ✅ (bench-gate green) | ✅ (ci.yml:469) | **None** — closeable now |
| B10 jsonrpc2 boundary | ✅ | n/a (structural) | n/a | **None** — verified clean; closeable now |

**Four of six (B5, B6, B9, B10) are effectively done** — the work is
confirming the evidence and flipping the checkbox. **Two (B7, B8) have working
machinery but fail their own "part of the release path" criterion** because no
GitHub workflow runs them. That is the real remaining B5–B10 surface.

## Per-blocker findings

### B5 — security evidence and auth hardening — CLOSEABLE

- All ten named tests in the claim-to-evidence map exist
  (`auth_security_test.go`, `auth_test.go`, `security_gate_test.go`) and the
  recorded subset command passes: `ok github.com/tmc/mcp 0.409s`.
- `go test -race ./...` is wired in `ci.yml` (race matrix, lines 328–350,
  including `cmd/mcp` and `cmd/mcp-probe`).
- Remaining: re-confirm acceptance criterion 3 — every claim in `SECURITY.md`
  maps to a named check, an explicit deferral, or a removed claim — is still
  accurate at HEAD, then mark closed. No code work identified.

### B6 — root-module dependency contract — CLOSEABLE NOW

- `scripts/check-root-dep-contract.sh` runs clean (`root runtime dependency
  contract satisfied`, exit 0) and is wired in `ci.yml:128` and `make
  check-deps`.
- Runtime modules are exactly stdlib + `golang.org/x/*` + the two approved R4
  exceptions (`gorilla/websocket`, `santhosh-tekuri/jsonschema/v5`).
  Test-only modules (`go-cmp`, `x/tools`, `rsc.io/script`) are correctly
  segregated.
- No remaining work. Closeable on its own merits.

### B7 — upstream conformance harness — WORK REMAINS (CI wiring)

- `scripts/mcp-conformance.sh` exists, is `make conformance`, and — importantly
  — has a **self-contained fixture mode**: with `MCP_CONFORMANCE_URL` unset it
  builds and starts `internal/integration_testing/conformance-server` and runs
  `npx -y @modelcontextprotocol/conformance@0.1.16 server --suite active`
  against it. `--dry-run` resolves cleanly (exit 0); node v24.14.1 is found.
- Gap against acceptance criterion 3 ("part of the release path, not a manual
  one-off"): **no workflow references conformance/npx/MCP_CONFORMANCE_URL.**
  The harness is reproducible but not gated.
- Remaining work:
  1. Add a CI job (or a release.yml step) that runs `scripts/mcp-conformance.sh`
     in self-fixture mode on a runner with node. Decide active-vs-all suite for
     the gate and pin an expected-failures file if the active suite isn't 100%.
  2. Record one passing run artifact in-tree as the bootstrap evidence (same
     pattern as the B9 baseline).
- Open question: is npm-in-CI acceptable for the release gate, or should the
  conformance run be a release-time manual step with a recorded transcript?
  This is a policy call, not a code blocker.

### B8 — non-Go interop baseline — WORK REMAINS (scope + CI wiring)

- `internal/integration_testing/typescript-sdk-interop/typescript_sdk_test.go`
  exists and **passes locally**: `TestTypeScriptSDKStdioSmoke` → `ok ... 2.152s`
  (real TS SDK client init + tools/list + tools/call over stdio). A second test
  `TestTypeScriptSDKStreamableHTTPSmoke` covers streamable HTTP. Tests
  self-skip when node/npm is absent.
- Two gaps against the acceptance criteria:
  1. **Transport scope (criterion 3).** The smoke covers **stdio + streamable
     HTTP**. The root public surface also exports **SSE** (`SSEClientTransport`/
     `SSEServerTransport`) and **WebSocket** (`WebSocketTransport`). The gate
     requires "every transport still in scope at tag time." This forces a
     decision that ties to synthesis **S3** (Streamable transport promotion is
     *deferred to v1.x*) — so the in-scope set must be settled first:
     - If SSE + WebSocket are in the v1 root surface, the TS smoke must add
       them (or B8 records an explicit per-transport deferral).
     - If S3 holds and streamable is *not* a v1-stable contract, the existing
       streamable smoke is bonus, and the gate hinges on SSE + WebSocket.
  2. **CI wiring (criterion 2/3).** No workflow runs the interop suite; it is
     `make`-only under `internal/integration_testing/Makefile` (GOWORK=off).
- Remaining work: settle the transport-scope question against S3, extend the
  smoke (or record deferrals) to match, then wire it into CI/release.

### B9 — performance baseline in CI — CLOSEABLE NOW

- `scripts/bench-gate.sh` runs clean (exit 0); both gated benchmarks are well
  inside tolerance at HEAD:
  - `BenchmarkServer_HandleRequest/PayloadSize_1024`: 195355 ns/op vs 219643
    baseline (0.89x, limit 5.0x); B/op 1.00x; allocs 1.00x.
  - `BenchmarkTokenValidation`: 40.65 ns/op vs 43.62 (0.93x); 0 B/op; 0 allocs.
- Baseline committed at `testdata/benchmarks/b9-baseline.txt`; gate wired in
  `ci.yml:469`.
- No remaining work. The committed baseline is a `darwin/arm64` bootstrap; a
  Linux refresh is a nice-to-have the gate text already flags as non-blocking
  (tolerances are deliberately conservative).

### B10 — jsonrpc2 boundary decision — CLOSEABLE NOW

- Repo-local `jsonrpc2/` directory is gone. No non-test root `.go` file
  references `github.com/tmc/mcp/jsonrpc2` (grep clean).
- `cmd/mcp-probe` consumes the upstream public package
  `github.com/modelcontextprotocol/go-sdk/jsonrpc` from its own nested module
  (`main.go:18`, `debug_test.go:7`, `_examples/simple_output.go:10`).
- The root build uses `golang.org/x/exp/jsonrpc2` (confirmed in the B6 runtime
  module list). All three acceptance criteria are met structurally. Closeable.

## Recommended order of operations

The remaining real work is small and concentrated in B7/B8. Suggested sequence
(leaf → policy-dependent, matching the project's bisect-safe ordering habit):

1. **Flip B6, B9, B10 to closed** in `v1-release-exemplary-gate.md` — pure
   doc edits; evidence already passes and is CI-wired (B6/B9) or structural
   (B10).
2. **B5**: re-walk the `SECURITY.md` claim-map for staleness, then close. No
   code expected.
3. **B7**: add a conformance CI/release step using the existing self-fixture
   script; record a bootstrap passing run. (Decide suite + npm-in-CI policy.)
4. **B8**: settle the transport-scope question against S3 first (this is a
   judgment call worth surfacing to the maintainer), then extend the TS smoke
   to the agreed in-scope transports and wire it into CI/release.

B7 and B8 share one cross-cutting decision: **is node/npm an acceptable
dependency of the release gate, or are these recorded-transcript release-time
steps?** Answering that unblocks both.

## What this scoping did NOT change

No code or gate-status edits were made in this pass — it is verification only.
The two B11–B14-style "the panel says it's broken" risks do not apply here:
B5–B10 are the maintainer's own gate, and every artifact it names was found and
exercised. The gap is wiring and one scope decision, not missing machinery.
